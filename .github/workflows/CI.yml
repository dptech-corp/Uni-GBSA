name: MMPBSA unittest
on: [push]

jobs:
  MMPBSA_unittest:
    runs-on: "ubuntu-latest"
    container: dockerymh/hmtpbsa:gmx
    
    steps:
    - name: checkout repo
      uses: actions/checkout@v2
        
    - name: install hmtpbsa
      shell: bash
      run: |
        source activate amber21
        conda install -y openmpi
        python setup.py install

    - name: run unittest
      shell: bash
      run: |
        source activate amber21
        cd tests
        bash cmd.sh
